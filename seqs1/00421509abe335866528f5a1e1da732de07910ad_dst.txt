final ResultSet rs = statement . executeQuery ( "select id, issue_code, magazine_id, year, label, sku, language," + "published, free, release_date, release_id, release_rank, summary, preview, latest_export " + "from issue " + "where NOT(test) " + "order by release_date DESC" )  
