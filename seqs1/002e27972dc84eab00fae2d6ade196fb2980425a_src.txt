private static String QUERY_STORAGE = "Neo4j"  
String createVertexTable = "CREATE TABLE IF NOT EXISTS " + VERTEX_TABLE + " (vertexId INT PRIMARY KEY AUTO_INCREMENT, " + "type VARCHAR(32) NOT NULL, " + "hash INT NOT NULL" + ")"  
String createEdgeTable = "CREATE TABLE IF NOT EXISTS " + EDGE_TABLE + " (edgeId INT PRIMARY KEY AUTO_INCREMENT, " + "type VARCHAR(32) NOT NULL ," + "hash INT NOT NULL, " + "srcVertexHash INT NOT NULL, " + "dstVertexHash INT NOT NULL" + ")"  
String query = "SELECT * FROM VERTEX WHERE vertexId = " + vertexId  
putEdge ( edge )  
